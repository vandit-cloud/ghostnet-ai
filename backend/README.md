Moved. Member 2's backend now lives at `app/backend/` — see `app/README.md`.

The app half was a separate repository until 2026-09-04, when it was merged in
with `git subtree add --prefix=app`, history intact. This placeholder stays only
so an old link or bookmark says where things went.
