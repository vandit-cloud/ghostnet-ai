__pycache__/
*.pyc
.venv/
venv/
backend/uploads/
backend/.env
.env
node_modules/
.next/
frontend/.env.local
*.log
.DS_Store

# --- build / tool artifacts (added 2026-09-04 at git init) -----------------
.pytest_cache/
tsconfig.tsbuildinfo
*.bak

# The AI package is installed EDITABLE from ai/ in this same repository
# (`pip install -e ./ai --no-deps`, run from the repository root), so its
# weights and calibrator live there and are gitignored there. Nothing to ignore
# here -- but note the backend cannot produce real detections without them.

# Claude Code runtime state (a stale PID/session lock, not project config)
.claude/scheduled_tasks.lock
